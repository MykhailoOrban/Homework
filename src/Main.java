import Shape.Circle;
import Shape.Shape;
import Shape.ShapeShowInfo;

public class Main {
    public static void main(String[] args) {
        ShapeShowInfo shape = new ShapeShowInfo(new Circle(3.5));

    }
}